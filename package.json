{
  "name": "boxcube-verify-bot",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "app-rakki": "cd app && npm start",
    "app": "cd frontend && npm start",
    "discord": "cd discord && npm start --ignore app",
    "dev": "concurrently --kill-others \"npm run app-rakki\" \"npm run discord\"",
    "test": "echo \"Error: no test specified\" && exit 1",
    "install-all": "npm install && (cd frontend && npm install --legacy-peer-deps) && (cd ../discord && npm install)"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^7.0.0"
  },
  "dependencies": {
    "buffer": "^6.0.3"
  }
}
